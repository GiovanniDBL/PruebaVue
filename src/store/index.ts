import { createStore } from 'vuex'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
    correr(){
      alert('cprrer mutations')
    }

  },
  actions: {
  },
  modules: {
  }
})
